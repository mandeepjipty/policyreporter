# Assignment 1: Best Threshold Finder## OverviewThis assignment implements a function to determine the best confidence threshold for a binary classifier.The selected threshold must meet a minimum recall of 0.9, and among those, the one with the highest precision is chosen.## Files- **best_threshold.py**: Contains the main function and an example demonstrating how to call it.- **README.md**: Provides an overview and instructions.## How to RunTo run the script, open a terminal in the `assignment1` directory and execute:```bashpython3 best_threshold.py